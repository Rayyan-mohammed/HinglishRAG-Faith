# Notebooks

Exploratory and prototyping work that doesn't belong in `src/` — prompt iteration, decomposition
edge-case testing, ad hoc retrieval sanity checks. If something here becomes a real pipeline
component, move it into `src/` and delete the notebook version.
